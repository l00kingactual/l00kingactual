CREATE CONSTRAINT unique_planet_name ON (p:Planet) ASSERT p.name IS UNIQUE;

CREATE CONSTRAINT unique_constellation_name ON (c:Constellation) ASSERT c.name IS UNIQUE;
CREATE CONSTRAINT unique_messier_object_name ON (m:MessierObject) ASSERT m.name IS UNIQUE;

CREATE CONSTRAINT unique_moon_name ON (m:Moon) ASSERT m.name IS UNIQUE;
CREATE CONSTRAINT unique_asteroid_name ON (a:Asteroid) ASSERT a.name IS UNIQUE;
CREATE CONSTRAINT unique_comet_name ON (c:Comet) ASSERT c.name IS UNIQUE;


// Create a node for Hydrogen
CREATE (h:Element {
    AtomicNumber: 1,
    Symbol: 'H',
    n: 1,
    l: 0,
    ml: 0,
    ms: 0.5,
    q: 0
});
